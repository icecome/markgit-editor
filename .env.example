# MarkGit Editor Environment Configuration

# GitHub OAuth 2.0 Device Authorization Flow
# 在 GitHub 开发者设置中创建 OAuth App 获取这些值
# https://github.com/settings/developers

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth 作用域，根据需要调整
# repo: 完全控制私有仓库
# workflow: 更新 GitHub Actions workflow
GITHUB_SCOPE=repo,workflow

# 会话配置
# 最大并发会话数（内存模式下）
MAX_CONCURRENT_SESSIONS=100

# 会话超时时间（小时）
# OAuth 令牌默认 1 小时过期
SESSION_TIMEOUT_HOURS=1

# Redis 配置（生产环境使用）
# 如果未设置 REDIS_URL，将使用内存存储（仅开发环境）
REDIS_URL=redis://localhost:6379/0

# Git 仓库配置
BLOG_GIT_SSH=
BLOG_BRANCH=main

# 部署配置（可选）
CMD_AFTER_PUSH=
ALLOWED_DEPLOY_SCRIPTS_DIR=

# SSH 密钥路径（如果使用 SSH 而不是 OAuth）
GIT_SSH_KEY_PATH=
